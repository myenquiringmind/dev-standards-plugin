"""Tooling scripts for the dev-standards-plugin framework.

Scripts in this package produce or validate derived artifacts the
framework's hooks and agents consume at runtime. They are not
themselves hooks — they run from the CLI (directly or via
``uv run python -m scripts.<name>``).
"""
